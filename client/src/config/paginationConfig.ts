/**
 * Centralized pagination configuration for client-side UI.
 *
 * Use DEFAULT_PAGE_SIZE in any component with paging logic.
 */
export const DEFAULT_PAGE_SIZE = 10;
export const MAX_PAGE_SIZE = 100;
